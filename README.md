# 🕵️ Mayoku (Telegram Mini App 2025)

**Концепция:** Современная, визуально богатая адаптация игры "Шпион" для Telegram Mini Apps.
**Ключевые фичи:** Real-time геймплей, красивые визуальные карточки, конструктор сценариев (UGC) и механика "Шериф" (голосование).

---

## 🚀 Функционал

### 👤 Игрок
*   **Auth:** Бесшовный вход через Telegram `initData`.
*   **Lobby:** Создание приватных комнат, инвайт друзей по ссылке.
*   **Gameplay:**
    *   Общий синхронизированный таймер.
    *   Карточки с артами локаций (вместо сухого текста).
    *   Интерфейс голосования за исключение игрока.
*   **Profile:** Статистика побед/поражений, история матчей.

### 🎨 Конструктор (UGC)
*   Создание своих наборов локаций (Decks).
*   Загрузка изображений для локаций (хранятся в MinIO).
*   Настройка ролей для каждой локации.
*   Публикация наборов (через модерацию).

### 🛡️ Админ
*   Модерация пользовательских наборов.
*   Управление жалобами.

---

## 🛠 Технический стек (Go v1.25.3)

### Backend
*   **Core:** Go 1.25.3
*   **Router:** [Chi](https://github.com/go-chi/chi) — легковесный, идиоматичный роутер.
*   **ORM:** [GORM](https://gorm.io/) — работа с БД (используем `AutoMigrate` для MVP).
*   **Logger:** [Yagalog](https://pkg.go.dev/github.com/Chelaran/yagalog) — структурированное логирование.
*   **WebSockets:** `gorilla/websocket` (или аналог) — движок реального времени.
*   **Config:** `cleanenv` или `viper`.

### Frontend
*   **Framework:** Next.js 15 (SPA Mode / Static Export).
*   **Language:** TypeScript.
*   **UI:** Tailwind CSS 4 + shadcn/ui.
*   **State:** Zustand (Client) + TanStack Query (Server).

### Infrastructure
*   **Database:** PostgreSQL 17 (постоянные данные).
*   **Cache/State:** Redis 7+ (состояние активных комнат, TTL).
*   **Storage:** MinIO (S3 Compatible) — хранение картинок.
*   **Containerization:** Docker Compose.

---

## 📂 Структура проекта (Standard Go Layout)

Используем `cmd/internal` паттерн, но без лишней слоистости. Группировка по доменам.

```text
/
├── cmd/
│   └── api/
│       └── main.go       # Точка входа: инициализация DB, роутера и запуск
│
├── internal/
│   ├── config/           # Загрузка конфигурации (.env)
│   ├── models/           # GORM структуры (User, Deck, Location)
│   ├── database/         # Подключение к Postgres, Redis, MinIO
│   │
│   ├── game/             # ИГРОВОЙ ДВИЖОК
│   │   ├── hub.go        # Управление комнатами (создание, удаление)
│   │   ├── room.go       # Логика игры (таймер, голосование, стейт)
│   │   └── client.go     # WS чтение/запись
│   │
│   └── api/              # HTTP СЛОЙ
│       ├── handlers/     # Обработчики (Auth, Decks, Upload)
│       ├── router.go     # Маршруты Chi и Middleware
│       └── server.go     # Запуск сервера
│
├── web/                  # Frontend (Next.js)
├── .env                  # Переменные окружения
└── docker-compose.yml    # Инфраструктура
```

## 🏗 Архитектура Данных

### PostgreSQL (GORM Models)
Основные сущности (internal/models/):

```go

type User struct {
    ID        uint   `gorm:"primaryKey"`
    TgID      int64  `gorm:"uniqueIndex"`
    Username  string
    AvatarURL string
    Decks     []Deck // Связь с созданными колодами
}

type Deck struct {
    ID        uint
    AuthorID  uint
    Name      string
    IsPublic  bool       `gorm:"default:false"`
    Locations []Location `gorm:"foreignKey:DeckID"`
}

type Location struct {
    ID       uint
    DeckID   uint
    Name     string
    ImageURL string   // Ссылка на MinIO
    Roles    []string `gorm:"serializer:json"` // ["Доктор", "Медсестра", ...]
}
```

### Redis (Game State)
Redis хранит "горячие" данные живых игр.

- Key: room:{uuid}
- TTL: 1 час (авто-очистка брошенных комнат).
- Value: JSON Snapshot игры.

```json

{
  "status": "playing",
  "timer_end": 1735567890,
  "location": { "name": "Casino", "img": "s3://..." },
  "spy_id": 12345,
  "players": [ ... ]
}
```

## 🔌 API & WebSocket Protocol
### HTTP Endpoints
- POST /api/auth — валидация initData, возврат JWT.
- GET /api/user/me — профиль.
- POST /api/decks — создание набора.
- POST /api/upload — загрузка картинки (Multipart Form -> MinIO).

### WebSocket Events
#### Client -> Server:

1. join_room { room_id }
2. vote_start { target_user_id }
3. vote_answer { yes/no }

#### Server -> Client:

1. room_update (полный список игроков при входе/выходе).
2. game_started (твоя роль, локация, время).
3. vote_initiated (поп-ап голосования).
4. game_over (результаты).

## 🏁 Roadmap (MVP)
1. Infra: Поднять docker-compose (Postgres, Redis, MinIO).
2. Auth: Реализовать проверку подписи Telegram initData.
3. Engine: Написать Hub и Room логику на Go.
4. Storage: Реализовать загрузку картинок в MinIO.
5. Front: Сверстать Лобби и Игровой экран.
